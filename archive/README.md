# Archive

This folder is for older material that should be kept for reference but is not
part of the active MATLAB source, Python package, examples, validation scripts,
or tests.

Use this folder for retired scripts, superseded experiments, old notes, and
previous outputs that are useful to preserve but should not define the current
project structure.

Do not put shared input data here if active MATLAB or Python code still reads
it.

`pre_matlab_v2_density_2026-08-20/` restores the Python version immediately
before the analytic MATLAB-v2 density port.
